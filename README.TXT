
Answers to Lab 8, Part I:

#9 The keySet() method returns a Set containing all the keys in a HashMap.

#13 The rooms will change. Command words object will be created through the parser.

#17 Yes, in order to define what the command does.

#18 In this case it would be more effictive to make all its methods static because since the commandWords
    class has no instance data, no object even needs to be created for it to work properly. The cons of this are that
    we move away from the object oriented design because that class would just be a container of methods.
